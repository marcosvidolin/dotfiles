require("vidolin.core")
require("vidolin.lazy")

-- function GetColor(group)
--   local syntaxID = vim.fn.hlID(group)
--   return vim.fn.synIDattr(syntaxID, 'fg', 'gui')
-- end
--
-- local color = GetColor('LineNr')
-- local colorN = GetColor('CursorLineNr')
-- print('cor: ' .. color .. colorN)
